<nav class="site-nav" role="navigation" aria-label="Main navigation">
    <div class="site-nav__inner">
        {% for link in navbarLinks %}
            <a href="{{ link.href }}" target="{{ link.target }}" class="site-nav__link">{{ link.label }}</a>
        {% endfor %}
    </div>
</nav>
